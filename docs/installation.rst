.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install finitio

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv finitio
    $ pip install finitio
